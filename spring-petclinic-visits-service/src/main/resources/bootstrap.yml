spring:
  cloud:
    config:
      uri: http://localhost:8888
      label: poc
  application:
    name: visits-service
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
